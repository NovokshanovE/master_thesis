\section{Результаты экспериментов и их интерпретация}
\label{sec:experiment-results}

\subsection{Полная выгрузка}

Измерения показывают, что прежний вариант хранилища, основанный на
\texttt{ranger},
значительно быстрее выполняет полную выгрузку таблицы. Это объясняется
тем, что он концептуально хранит уже подготовленный линейный набор
записей и может отдавать его почти как копию готового массива.
Шардированный ART, напротив, должен обойти все шарды, декодировать
ключи, собрать результат и (по умолчанию) отсортировать его.

Конкретные численные результаты приведены в
табл.~\ref{tab:bench-dump}. На малых таблицах ($n=1000$, $n=10000$)
разница составляет два--три порядка, а на $n=100000$ при выключенной
сортировке разрыв сокращается, оставаясь, однако, значительным.

\begin{table}[ht]
\centering
\caption{Бенчмарк \texttt{Dump} для IPv4 \texttt{/24}}
\label{tab:bench-dump}
\small
\begin{tabular}{lrrr}
\toprule
Бэкенд (n) & Время & Память & Аллокаций \\
\midrule
ranger, $n=1000$    & $\approx 2{,}6\,$мкс/op & $8\,$KiB    & $1$ \\
ranger, $n=10\,000$ & $\approx 30\,$мкс/op    & $80\,$KiB   & $1$ \\
ranger, $n=100\,000$ & $\approx 245\text{--}275\,$мкс/op & $784\,$KiB & $1$ \\
ART, $n=1000$        & $\approx 337\,$мкс/op  & $\approx 270\,$KiB & $\approx 3970$ \\
ART, $n=10\,000$     & $\approx 3{,}2\,$мс/op & $\approx 3{,}0\,$MiB & $\approx 35\,000$ \\
ART, $n=100\,000^{\ast}$ & $\approx 13{,}4\text{--}13{,}8\,$мс/op & $\approx 33{,}4\,$MiB & $\approx 351\,000$ \\
\bottomrule
\end{tabular}\\[2pt]
{\footnotesize $^{\ast}$ ART, $n=100\,000$ -- режим без сортировки
дампа (\texttt{WithDumpSort(false)}).}
\end{table}

Эти данные фиксируют существенный проигрыш ART по полной выгрузке:
при $n=10^5$ операция занимает $13$--$15$ мс против сотен микросекунд у
\texttt{ranger}. Для целевого профиля нагрузки это не опровергает
гипотезу, поскольку \texttt{Dump} не является основным рабочим режимом
потоковой модели, но задаёт ограничение для сценариев частых полных
выгрузок.

\subsection{Инкрементальное обновление}

С точки зрения постановки задачи этот раздел -- центральный: он
показывает, что потоковое применение одиночных BGP-изменений к
in-memory базе технически осуществимо, а не только объявлено в
архитектуре. Именно на операции одиночного \texttt{Upsert} новый подход
демонстрирует решающее преимущество (табл.~\ref{tab:bench-upsert}). У
ranger каждый \texttt{Upsert} пересобирает весь список и вызывает
\texttt{Replace}, что приводит к полной перестройке внутренней
структуры. У ART та же операция выполняется локально в одном шарде,
без вмешательства в остальные.

\begin{table}[ht]
\centering
\caption{Бенчмарк одиночного \texttt{Upsert} поверх заполненной
таблицы (IPv4)}
\label{tab:bench-upsert}
\small
\begin{tabular}{lrr}
\toprule
$n$ & ranger & ART (32 шарда) \\
\midrule
$1000$       & $\approx 3{,}0\,$мс/op    & $\approx 128\,$нс/op \\
$10\,000$    & $\approx 43\,$мс/op       & $\approx 125\,$нс/op \\
$100\,000$   & $\approx 587\text{--}616\,$мс/op & $\approx 165\text{--}169\,$нс/op \\
\bottomrule
\end{tabular}
\end{table}

Разница достигает примерно $\textbf{шести порядков}$ при $n=10^5$:
с~$\sim\!600$ мс/op до~$\sim\!170$ нс/op. Это главный эмпирический
аргумент в пользу потоковой модели: без такой стоимости \texttt{Upsert}
поддержание таблицы по одному событию не имело бы эксплуатационного
смысла. Новая
архитектура качественно меняет допустимую модель эксплуатации: если при
\texttt{ranger} поток BGP-изменений практически несовместим с локальным
обновлением, то при шардированном ART инкремент становится естественным
базовым режимом -- уже внутри выделенного микросервиса с общим API.

\subsection{Lookup по IP}

Это вспомогательный разрез относительно центральной цели работы
(микросервис и поток обновлений), но он фиксирует цену точечного чтения
после выбора структуры. По операции \texttt{Lookup} картина оказывается более
неоднородной. Для IPv4
прежний вариант хранилища показывает меньшую задержку, а ART остаётся в зоне
приемлемых, но более высоких значений. Для IPv6 разрыв становится
ещё заметнее: текущая реализация ART выполняет \texttt{Lookup} существенно
медленнее (табл.~\ref{tab:bench-lookup}). Объяснение в том, что ART
при поиске перебирает все возможные длины масок -- до~33 для IPv4 и
до~129 для IPv6.

\begin{table}[ht]
\centering
\caption{Бенчмарк \texttt{Lookup} (IPv4 \texttt{/32} и IPv6
\texttt{/128}, $n \approx 10^5$)}
\label{tab:bench-lookup}
\small
\begin{tabular}{lrr}
\toprule
Сценарий       & ranger                & ART (32 шарда) \\
\midrule
IPv4 \texttt{/32} & $151\,$нс/op       & $400\text{--}500\,$нс/op \\
IPv6 \texttt{/128} & $168\text{--}178\,$нс/op & $3700\text{--}4000\,$нс/op \\
\bottomrule
\end{tabular}
\end{table}

Это важное ограничение текущей реализации. Оно показывает, что
шардированный ART не является универсальным победителем по всем
метрикам. Однако результат не опровергает гипотезу работы: цель
состояла не в максимизации одной операции \texttt{Lookup}, а в
достижении баланса между чтением и инкрементальной актуализацией.
Возможные пути оптимизации \texttt{Lookup} для ART рассматриваются в
главе~\ref{sec:limitations}.

\subsection{Влияние числа шардов}

Эксперименты показывают, что увеличение числа шардов не даёт линейного
ускорения всех операций. Для \texttt{Lookup} выигрыш часто невелик
или находится в пределах шума. Для \texttt{Dump} слишком большое
число шардов может даже ухудшать результат из-за роста служебных
издержек (табл.~\ref{tab:bench-shards}). Для update-сценариев
умеренное шардирование оказывается полезным, потому что снижает
конкуренцию между независимыми модификациями.

\begin{table}[ht]
\centering
\caption{Измерение при разном числе шардов: \texttt{Dump} ART, IPv4, без
сортировки}
\label{tab:bench-shards}
\small
\begin{tabular}{lrr}
\toprule
$K$ (шардов) & $n=5000$ & $n=100\,000$ \\
\midrule
$1$  & --                    & $\approx 12{,}2\text{--}12{,}6\,$мс/op \\
$8$  & $\approx 1{,}45\text{--}1{,}47\,$мс/op & $\approx 10{,}9\text{--}11{,}6\,$мс/op \\
$16$ & $\approx 1{,}50\text{--}1{,}56\,$мс/op & $\approx 10{,}9\text{--}11{,}6\,$мс/op \\
$32$ & $\approx 1{,}58\text{--}1{,}59\,$мс/op & $\approx 13{,}9\text{--}15{,}3\,$мс/op \\
$64$ & $\approx 1{,}76\text{--}1{,}78\,$мс/op & $\approx 13{,}9\text{--}14{,}8\,$мс/op \\
\bottomrule
\end{tabular}
\end{table}

Следовательно, число шардов является параметром настройки, а не
правилом «чем больше, тем лучше». Практически разумной выглядит зона
умеренного шардирования ($K \in [8;32]$), а не экстремальные
значения.

\subsection{Поведение IPv6}

IPv6 заслуживает отдельного внимания. По \texttt{Dump} разрыв между
ranger и ART сохраняется на том же уровне, что и для IPv4: примерно
$200$--$250$ мкс против $12{,}5$--$13{,}7$ мс. По \texttt{Upsert}
картина та же: $\sim\!889$ мс у ranger против $\sim\!173$--$182$ нс у
ART, что снова даёт разницу более чем в пять порядков. По
\texttt{Lookup} ranger быстрее в десятки раз, что объясняется
значительно большим числом перебираемых длин масок в текущей
реализации ART. Этот результат важен как ограничение реализации, а не
как свойство самого подхода.

\subsection{Сводный итог сравнения}

Сводка по сценариям приведена в табл.~\ref{tab:bench-summary}.

\begin{table}[ht]
\centering
\caption{Итоговая сводка: какой вариант хранилища выигрывает в каком сценарии}
\label{tab:bench-summary}
\small
\begin{tabular}{>{\raggedright\arraybackslash}p{4.0cm}lp{6.5cm}}
\toprule
Сценарий & Победитель & Комментарий \\
\midrule
Полная выгрузка
  & ranger
  & готовый линейный массив; ART платит за обход и аллокации \\
Одиночный \texttt{Upsert}
  & ART
  & 5--6 порядков преимущества; решающий фактор для потокового режима \\
\texttt{Lookup} IPv4
  & ranger
  & разница в 2--3 раза; для прикладного сервиса допустимо \\
\texttt{Lookup} IPv6
  & ranger
  & разрыв в десятки раз; ограничение текущей реализации LPM \\
Поток обновлений в целом
  & ART
  & структура с полной перестройкой принципиально несовместима с моделью \\
Bootstrap / \texttt{Replace}
  & сопоставимо
  & обе реализации справляются за приемлемое время \\
\bottomrule
\end{tabular}
\end{table}

Содержательно из сводки следуют четыре наблюдения:

\begin{enumerate}
\item прежнее решение лучше оптимизировано под пакетный сценарий
«получить полный снимок и отдать целиком»;
\item новое решение -- под потоковый сценарий «применить локальную
правку»;
\item новый вариант хранилища платит за изменяемость более дорогими
\texttt{Dump} и частью \texttt{Lookup};
\item только новый вариант хранилища делает практически выполнимым непрерывное
поддержание таблицы в актуальном состоянии.
\end{enumerate}

Четвёртый пункт фиксирует главный системный вывод: потоковое обновление
in-memory таблицы становится эксплуатационно реальной опцией. На этом
фоне выделение функции в микросервис и единый gRPC контракт для
нескольких продуктов -- самостоятельный архитектурный слой; шардированный
ART снижает CPU-затраты потокового обновления по сравнению с пакетной
перестройкой \texttt{ranger}.

\subsection{Совокупная стоимость потокового профиля нагрузки}

Чтобы перевести микробенчмарки в эксплуатационно содержательную
плоскость для уже выделенного микросервиса, полезно построить простую
модель совокупной стоимости для типичных профилей с потоком обновлений.
Рассмотрим установившийся режим, в котором:

\begin{itemize}
\item в системе хранится таблица из $n$ префиксов
($n \in \{10^4;\,10^5\}$);
\item за единицу времени происходит $\lambda$ инкрементальных
обновлений (Upsert/Delete);
\item клиенты выполняют $\mu$ запросов \texttt{Lookup} в секунду;
\item операция полного \texttt{Dump} вызывается с частотой $\nu$ в
секунду.
\end{itemize}

Тогда суммарную CPU-нагрузку на хранилище можно оценить как

\[
C(\lambda,\mu,\nu) =
   \lambda \cdot t_{\mathrm{upsert}}(n)
 + \mu    \cdot t_{\mathrm{lookup}}(n)
 + \nu    \cdot t_{\mathrm{dump}}(n),
\]

где $t_{\mathrm{upsert}}$, $t_{\mathrm{lookup}}$ и
$t_{\mathrm{dump}}$ -- это средние времена соответствующих операций,
взятые из табл.~\ref{tab:bench-dump}--\ref{tab:bench-shards}.

Подставим типичные значения для $n = 10^5$:

\begin{itemize}
\item ranger: $t_{\mathrm{upsert}} \approx 6{,}0 \cdot 10^{-1}$ с;
$t_{\mathrm{lookup}} \approx 1{,}5 \cdot 10^{-7}$ с;
$t_{\mathrm{dump}} \approx 2{,}6 \cdot 10^{-4}$ с;
\item шард. ART: $t_{\mathrm{upsert}} \approx 1{,}7 \cdot 10^{-7}$ с;
$t_{\mathrm{lookup}} \approx 4{,}5 \cdot 10^{-7}$ с;
$t_{\mathrm{dump}} \approx 1{,}4 \cdot 10^{-2}$ с.
\end{itemize}

Сравнение совокупной стоимости для двух представительных профилей
нагрузки приведено в табл.~\ref{tab:bench-cost}.

\begin{table}[ht]
\centering
\caption{Оценка совокупной CPU-стоимости в установившемся режиме при
$n = 10^5$ (приближённо, CPU-с/с $=$ доля одного CPU-ядра на каждую
секунду физического времени)}
\label{tab:bench-cost}
\small
\begin{tabular}{lrr}
\toprule
Сценарий & ranger & шард. ART \\
\midrule
$\lambda{=}10$, $\mu{=}10^4$, $\nu{=}10^{-2}$
	   & $\approx 6{,}00$ CPU-с/с
	   & $\approx 0{,}0045$ CPU-с/с \\
$\lambda{=}10^3$, $\mu{=}10^5$, $\nu{=}10^{-2}$
	   & $\approx 600$ CPU-с/с
	   & $\approx 0{,}047$ CPU-с/с \\
\bottomrule
\end{tabular}
\end{table}

Числа в табл.~\ref{tab:bench-cost} имеют ясную интерпретацию. В
обоих случаях ranger требует более чем одной полной CPU-секунды на
каждую секунду физического времени уже при умеренной интенсивности
обновлений; для второго профиля оценка по CPU-секундам соответствует
сотням CPU-ядер только на обработку изменений, что эксплуатационно
неприемлемо. ART-вариант, напротив, в обоих случаях остаётся на уровне
долей одного ядра, что показывает возможность использовать его как
источник состояния с потоковым обновлением.

Более того, в этой модели хорошо видно, что доминирующая статья
расходов меняется. Для варианта с полной перестройкой это всегда обновления
(независимо от наличия читателей), для потокового варианта -- скорее
\texttt{Lookup} и редкие \texttt{Dump}. Это качественно меняет ту
сторону системы, на которой имеет смысл проводить дальнейшую
оптимизацию.

Если переложить те же цифры в плоскость требований к задержке
актуализации, различие становится более выраженным. Пусть допустимая
задержка актуализации таблицы $T_{\mathrm{stale}} \le 1$~с. Чтобы
удерживать такой лаг, пакетной модели пришлось бы
перестраивать всю таблицу по несколько раз в секунду; при
$n = 10^5$ это $\sim$600 мс CPU-работы постоянно. Для потоковой
модели то же требование достигается за счёт обработки отдельных событий:
каждое обновление применяется за единицы микросекунд или быстрее. Эти
два варианта находятся в разных эксплуатационных режимах: первый при
умеренной интенсивности обновлений не способен удерживать задержку
актуализации на уровне одной секунды без непропорционального расхода CPU.

%==============================================================
